<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.x.mapper.ProvinceMapper">
    <insert id="insertProvince" >
        INSERT INTO province (adcode, name, center, centroid, children, the_geom)
        VALUES (#{adcode}, #{name}, null , null, #{children}, st_asgeojson(#{the_geom}))
    </insert>
</mapper>
